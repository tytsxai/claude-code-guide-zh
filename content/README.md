# 完整中文指南（待生成）

> 此文件是占位符。配置好 `DEEPSEEK_API_KEYS` 后运行一次同步流水线，
> 本文件会被上游 `README.md` 的完整中文译文自动覆盖：
>
> ```bash
> set -a; source .env; set +a
> python -m tools.sync --full
> ```
>
> 之后 GitHub Actions 会每小时自动增量同步。详见仓库根目录 [`README.md`](../README.md)。
